$kind: http-request
url: "{{baseUrl}}/sessions/{{sessionId}}/messages"
method: GET
headers:
  Authorization: Bearer {{tokenB}}
scripts:
  - type: afterResponse
    code: pm.test('Status is 404 (not the other users data)', () => pm.response.to.have.status(404));
    language: text/javascript
order: 12000
